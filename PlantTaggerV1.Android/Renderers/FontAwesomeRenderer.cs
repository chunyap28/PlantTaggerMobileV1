using System;
//using Android.Content;
//using Android.Graphics;
//using Xamarin.Forms;
//using Xamarin.Forms.Platform.Android;
//using FontAwesomeXamarin;

//[assembly: ExportRenderer(typeof(FontAwesome), typeof(FontAwesomeRenderer))]
namespace PlantTaggerV1.Droid.Renderers
{/*
    public class FontAwesomeRenderer : LabelRenderer
    {
        public FontAwesomeRenderer(Context context) : base(context)
        {
        }

        protected override void OnElementChanged(ElementChangedEventArgs<Label> e)
        {
            base.OnElementChanged(e);
            if (e.OldElement == null)
            {
                //The ttf in /Assets is CaseSensitive, so name it FontAwesome.ttf
                Control.Typeface = Typeface.CreateFromAsset(Context.Assets, "FontAwesome.ttf");
            }
        }
    }*/
}
